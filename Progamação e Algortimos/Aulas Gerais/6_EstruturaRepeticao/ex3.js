// 03) Fazer um programa que receba um valor n no teclado e determine o maior. A condição de término do programa é quando o usuário digitar zero.

const prompt = require("readline-sync");

let digitoVerifica = 0;
let digitoTemp = 0;

do {
    digitoVerifica = Number(prompt.question("Digite um valor para ser avaliado (Digite 0 para encerrar):"))

    if (digitoVerifica >)
}